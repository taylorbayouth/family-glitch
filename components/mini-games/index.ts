/**
 * Mini-Game UI Components
 *
 * Visual components for interactive mini-game challenges.
 * Each mini-game has its own UI but follows a common pattern:
 * - Receives context from the apparatus (play page)
 * - Handles its own AI calls for scoring
 * - Returns a MiniGameResult via onComplete
 */

export { TriviaChallengeUI } from './TriviaChallengeUI';
export { PersonalityMatchUI } from './PersonalityMatchUI';
